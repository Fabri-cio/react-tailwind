const BotonEliminar = ({ onClick }) => {
    return (
      <button onClick={onClick} className="btn-actualizar">
        Eliminar
      </button>
    );
  };
  
  export default BotonEliminar; // Exporta el componente
  